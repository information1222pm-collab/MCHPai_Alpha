"""taxonomy_validation_lab — does a candidate population *exist*?

Phase 7 found candidate clusters. One session, one feature set, one clustering is
not enough to believe them. This lab asks the prior question — *existence*, not
identity — and refuses to name anything until reality reproduces it.

The discipline:

* clusters keep **anonymous** ids (Cluster0, Cluster1, …). Names create
  certainty; certainty creates blindness.
* a cluster is believed only when its **geometry, membership, profile and
  behavior** reproduce across fresh, independent samples and resampling.
* nature gives gradients, not islands — soft separation (silhouette ~0.2) is
  expected; we measure *stability*, not crispness.

Names are privileges granted by stability. Existence precedes naming; behavior
precedes stories.
"""
