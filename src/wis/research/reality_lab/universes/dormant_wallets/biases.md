# Biases — Dormant wallets

Every sampling method distorts. Naming the distortion is part of the science: a distribution is only interpretable alongside its biases.

- fundamentally hard to sample — recent-activity discovery cannot see them
- requires a pre-existing wallet universe
- 'dormant' threshold is arbitrary
