# models/lightgbm

LightGBM trainer — default for the **risk_score** regression target. Fast, handles many features, native categorical support.

Implements the `Trainer`/`Model` interfaces in `models/base.py`.
