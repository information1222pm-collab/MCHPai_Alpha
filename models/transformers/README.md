# models/transformers

Transformer sequence models over per-wallet/token event streams (buys, sells, liquidity). Captures temporal order classical features miss.

Implements the `Trainer`/`Model` interfaces in `models/base.py`.
