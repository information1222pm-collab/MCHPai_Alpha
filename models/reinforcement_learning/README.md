# models/reinforcement_learning

RL (PPO) for **execution & sizing policy** — learns tip/slippage/size decisions against a simulated fill model. Off the live path until proven in shadow.

Implements the `Trainer`/`Model` interfaces in `models/base.py`.
