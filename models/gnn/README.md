# models/gnn

Graph Neural Networks over the Neo4j wallet graph (export → PyTorch Geometric). Learns wallet/cluster **embeddings** that feed downstream models.

Implements the `Trainer`/`Model` interfaces in `models/base.py`.
