# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Environments
.venv/
venv/
.env

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# OS / editor
.DS_Store
*.swp

# Live observation artifacts (real wallet data; reproducible, not source)
sessions/
*.helius.jsonl

# Daily scrape: keep lightweight summaries in-repo; full CSVs are artifacts /
# go to the durable rating store (Postgres). Don't commit the bulky CSVs.
ratings/daily/wallets-*.csv
