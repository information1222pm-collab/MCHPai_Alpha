# Daily scrape output

Lightweight daily summaries are committed here by the `daily-wallet-scrape`
workflow. Full per-wallet CSVs are git-ignored (uploaded as 90-day artifacts and,
if `WIS_PG_DSN` is set, upserted to a durable Postgres rating store).
